
# Planned Features
- [x] steam videos instead of loading them into MEM
- [ ] add options to option tab
    - [ ] Grid Size (maybe min max so that if we add a dynamic difficulty it doesn't get to hard or to easy)
    - [ ] Flash Hint Duration (0 ms for hardcore no flash, 500ms for obvious flash)
    - [ ] Zoom Sensitivity (ZOOM_STEP)
    - [ ] Master Default Volume
    - [ ] Display Mode (V-Sync vs. Uncapped)
    - [ ] Save Location (CUSTOM_PATH):
    - [ ] Clear Cache / Temp Files: A big red button that manually triggers utils.clear_temp_folders()

- [ ] Implement a scoring system based on time and move count.
- [ ] load more images at a time to avoid download bottleneck

# Bugs to Fix
- [ ] None
